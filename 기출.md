# 기출을 에이전트에게 넘기는 방법

SWEA는 로그인해야 해서 **URL만 주면 문제를 못 본다.**  
아래 순서로 가져오면 설계·복잡도 이야기를 바로 할 수 있다.

## 베스트 순서

1. **텍스트를 로컬 파일로 저장** → 채팅에서 `@기출/파일이름.md` (가장 좋음)
2. **채팅에 텍스트 붙여넣기** — 짧을 때
3. **그림·격자만 스크린샷** — 표/맵/예시 그림
4. **URL만** — 하지 말 것

제약 숫자(`N`, 호출 횟수, 값 범위)와 **함수 시그니처**는 반드시 텍스트다.  
스크린샷만 있으면 숫자 오인식이 나고, B형은 그 숫자로 자료구조를 고른다.

## 파일로 저장할 때

1. `기출/` 폴더를 만든다. (gitignore라 GitHub에 안 올라간다)
2. `기출/[문제이름].md` 에 아래 템플릿을 채운다.
3. 그림이 있으면 같은 채팅에 캡처를 **추가로** 첨부한다.
4. 채팅: `@기출/문제이름.md` 보고 B형처럼 설계부터 하자.

정답 코드·해설은 가져오지 않는다. 요구사항·API·제약·샘플만.

---

## 붙여넣기 템플릿 (이것만 채우면 충분)

```
# [Pro] 문제 이름

- SWEA 번호/클럽:
- 시간 제한:
- 메모리:
- 만점 / 부분점수:

## 함수 (시그니처 그대로)

void init(...);
...

## 제약 (숫자 그대로)

- N, M, 값 범위
- 각 함수 호출 횟수
- 전체 테스트케이스 수

## 각 함수가 하는 일

- init:
- (명령1):
- (명령2):

## 규칙 / 예외

(겹침, 삭제, 우선순위, 동시에 일어나는 일 등)

## 샘플

(입력이 명령 나열이면 그 흐름. 격자면 작은 예시.)
```

Main.java 샘플 소스가 있으면 같이 두면 `init` 다음에 명령이 어떻게 들어오는지만 파악하기 좋다.  
로컬에서만 쓰고, public 저장소에는 올리지 않는다.
